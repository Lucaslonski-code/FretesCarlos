// Bairros de morretes — valores temporários (200) a serem definidos manualmente.
export const MORRETES_PRICING: Record<string, number> = {
  "Centro Histórico": 200,
  "Porto de Cima": 200,
  "Vila das Flores": 200,
};
